<!DOCTYPE html>
<html>
    <%- include('../partials/head')%>
    
    <body>
        <%- include('../partials/header')%>
        <main>
            <section>
             <label>lista de autos bien al piso</label> 
                <ul>
                    <% productosJSON.forEach(function(productos){  %>
                        <%if (productos.estado == "true"){%>
                        <li>
                        <a><%=productos.marca%> <%=productos.modelo%></a>
                        </li>
                    <%  }}); %>
                </ul>
            </section>    
        </main>
    </body>
</html>